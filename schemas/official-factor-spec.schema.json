{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://evozeus.dev/schemas/official-factor-spec.schema.json",
  "title": "EvoZeus Official Factor Spec",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "stability",
    "factor_id",
    "version",
    "title",
    "summary",
    "compatibility",
    "governance",
    "input_contract",
    "evidence_contract",
    "output_contract",
    "examples"
  ],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "stability": {
      "type": "string",
      "const": "official"
    },
    "factor_id": {
      "type": "string",
      "pattern": "^[a-z0-9]+([.-][a-z0-9]+)*$"
    },
    "version": {
      "type": "string",
      "pattern": "^v?[0-9]+\\.[0-9]+\\.[0-9]+(-[A-Za-z0-9.-]+)?$"
    },
    "title": {
      "type": "string",
      "minLength": 3
    },
    "summary": {
      "type": "string",
      "minLength": 10
    },
    "compatibility": {
      "type": "object",
      "additionalProperties": false,
      "required": ["evozeus_protocol"],
      "properties": {
        "evozeus_protocol": {
          "type": "string"
        }
      }
    },
    "governance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["owner"],
      "properties": {
        "owner": {
          "type": "string"
        }
      }
    },
    "input_contract": {
      "type": "object",
      "additionalProperties": false,
      "required": ["event_model", "required_fields"],
      "properties": {
        "event_model": {
          "type": "string"
        },
        "required_fields": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          }
        }
      }
    },
    "evidence_contract": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ref_format", "privacy"],
      "properties": {
        "ref_format": {
          "type": "string"
        },
        "privacy": {
          "type": "string"
        }
      }
    },
    "output_contract": {
      "type": "object",
      "additionalProperties": false,
      "required": ["statuses", "fields"],
      "properties": {
        "statuses": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "enum": ["matched", "not_matched", "error"]
          }
        },
        "fields": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          }
        }
      }
    },
    "examples": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "input", "expected_status"],
        "properties": {
          "name": {
            "type": "string"
          },
          "input": {
            "type": "string"
          },
          "expected_status": {
            "type": "string",
            "enum": ["matched", "not_matched", "error"]
          }
        }
      }
    }
  }
}
