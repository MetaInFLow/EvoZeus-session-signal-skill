{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://evozeus.dev/schemas/factor-release-manifest.schema.json",
  "title": "EvoZeus Factor Release Manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "pack_id",
    "version",
    "source_review",
    "artifact",
    "checksum",
    "compatibility",
    "review_state",
    "attestation"
  ],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "pack_id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "version": {
      "type": "string",
      "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+(-[A-Za-z0-9.-]+)?$"
    },
    "source_review": {
      "type": "object",
      "additionalProperties": false,
      "required": ["lab_path", "reviewer", "review_date"],
      "properties": {
        "lab_path": {
          "type": "string"
        },
        "reviewer": {
          "type": "string"
        },
        "review_date": {
          "type": "string",
          "format": "date"
        }
      }
    },
    "artifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "type"],
      "properties": {
        "path": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": ["pack", "scanner-pack", "manifest-only"]
        }
      }
    },
    "checksum": {
      "type": "object",
      "additionalProperties": false,
      "required": ["algorithm", "path"],
      "properties": {
        "algorithm": {
          "type": "string",
          "enum": ["sha256"]
        },
        "path": {
          "type": "string"
        }
      }
    },
    "compatibility": {
      "type": "object",
      "additionalProperties": false,
      "required": ["evozeus_protocol", "runtime"],
      "properties": {
        "evozeus_protocol": {
          "type": "string"
        },
        "runtime": {
          "type": "string"
        }
      }
    },
    "review_state": {
      "type": "string",
      "enum": ["promoted", "deprecated", "yanked"]
    },
    "attestation": {
      "type": "string"
    },
    "notes": {
      "type": "string"
    }
  }
}
